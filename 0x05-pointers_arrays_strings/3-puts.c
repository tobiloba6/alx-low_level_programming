#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string followed by a new line
 * @str: string to be printed
 */

void _puts(char *str)
{
	int i = 0;

	while (*(str + n) != '\0')
	{
		putchar(*(str + n));
		n++;
	}
	putchar(10);
}
